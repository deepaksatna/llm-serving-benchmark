# vLLM - Mistral-7B-Instruct v0.3
# High-throughput LLM serving with PagedAttention
# Throughput: ~28-30 tok/s on NVIDIA A10
apiVersion: v1
kind: Service
metadata:
  name: vllm-mistral-svc
  namespace: nim-bench
  labels:
    app: vllm-mistral
    framework: vllm
spec:
  selector:
    app: vllm-mistral
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-mistral
  namespace: nim-bench
  labels:
    app: vllm-mistral
    framework: vllm
    model: mistral-7b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-mistral
  template:
    metadata:
      labels:
        app: vllm-mistral
        framework: vllm
    spec:
      serviceAccountName: nim-bench-sa
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 0
      containers:
      - name: vllm
        image: vllm/vllm-openai:v0.6.4
        command: ["python3", "-m", "vllm.entrypoints.openai.api_server"]
        args:
        - --model=/models/mistralai--Mistral-7B-Instruct-v0.3
        - --served-model-name=mistral-7b-instruct
        - --port=8000
        - --host=0.0.0.0
        - --dtype=float16
        - --trust-remote-code
        env:
        - name: TRANSFORMERS_OFFLINE
          value: "1"
        - name: HF_HUB_OFFLINE
          value: "1"
        ports:
        - containerPort: 8000
          name: http
        resources:
          requests:
            memory: "32Gi"
            cpu: "8"
            nvidia.com/gpu: "1"
          limits:
            memory: "64Gi"
            cpu: "16"
            nvidia.com/gpu: "1"
        volumeMounts:
        - name: fss-models
          mountPath: /models
          readOnly: true
        - name: shm
          mountPath: /dev/shm
        - name: results
          mountPath: /results
      volumes:
      - name: fss-models
        hostPath:
          path: /mnt/coecommonfss/llmcore/LLM-training/offline/image/models
          type: Directory
      - name: shm
        emptyDir:
          medium: Memory
          sizeLimit: "16Gi"
      - name: results
        hostPath:
          path: /mnt/coecommonfss/llmcore/2026-NIM-vLLM_LLM/results/vllm
          type: DirectoryOrCreate
      nodeName: 10.0.10.101  # GPU Node 2
      tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
